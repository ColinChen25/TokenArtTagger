<Window x:Class="TokenArtTagger.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TokenArtTagger.App"
        xmlns:vm="clr-namespace:TokenArtTagger.App.ViewModels"
        mc:Ignorable="d"
        Title="{Binding AppTitle}" Height="860" MinHeight="680" Width="1380" MinWidth="1060"
        Background="#F4F3EF"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        PreviewKeyDown="Window_PreviewKeyDown"
        SizeChanged="Window_SizeChanged"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <local:StringIsNullOrWhiteSpaceToVisibilityConverter x:Key="StringIsNullOrWhiteSpaceToVisibilityConverter" />
        <Style x:Key="PanelTitle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#252525" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="ScanButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsScanReminderActive}" Value="True">
                    <Setter Property="Background" Value="#FFF1D6" />
                    <Setter Property="BorderBrush" Value="#B7791F" />
                    <Setter Property="BorderThickness" Value="2" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TagButton" TargetType="Button">
            <Setter Property="Width" Value="82" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Margin" Value="0,0,6,6" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="TextBlock.TextAlignment" Value="Center" />
            <Setter Property="AllowDrop" Value="True" />
            <EventSetter Event="Drop" Handler="TagButton_Drop" />
            <EventSetter Event="PreviewDragOver" Handler="TagButton_PreviewDragOver" />
        </Style>
        <Style x:Key="BucketButton" TargetType="Button" BasedOn="{StaticResource TagButton}">
            <Setter Property="Width" Value="94" />
            <Setter Property="MinHeight" Value="34" />
        </Style>
        <Style x:Key="TileItem" TargetType="ListBoxItem">
            <Setter Property="Width" Value="178" />
            <Setter Property="Height" Value="230" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TileBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="5,2" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="MinHeight" Value="20" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="SelectableDetailText" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Margin" Value="0,4,0,12" />
        </Style>
        <Style x:Key="SelectablePreviewMessageText" TargetType="TextBox" BasedOn="{StaticResource SelectableDetailText}">
            <Setter Property="Foreground" Value="#9C1F1F" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAlreadyNormalized}" Value="True">
                    <Setter Property="Foreground" Value="#245C37" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <DataTemplate x:Key="SelectedImageDetailsTemplate" DataType="{x:Type vm:ImageItemViewModel}">
            <StackPanel>
                <TextBlock Text="Current filename" FontWeight="SemiBold" />
                <TextBox Text="{Binding FileName, Mode=OneWay}" Style="{StaticResource SelectableDetailText}" />
                <TextBlock Text="Full path" FontWeight="SemiBold" />
                <TextBox Text="{Binding FullPath, Mode=OneWay}" Style="{StaticResource SelectableDetailText}" FontWeight="SemiBold" />
                <Button Style="{StaticResource ToolButton}" Margin="0,0,0,6" Content="Show in Explorer" Click="ShowSelectedInExplorer_Click" />
                <Separator Margin="0,12" />
                <TextBlock Text="Parsed tags" FontWeight="SemiBold" />
                <TextBox Text="{Binding ParsedTagsText, Mode=OneWay}" Style="{StaticResource SelectableDetailText}" />
                <TextBlock Text="Current tags" FontWeight="SemiBold" />
                <TextBox Text="{Binding CurrentTagsText, Mode=OneWay}" Style="{StaticResource SelectableDetailText}" />
                <TextBlock Text="Proposed filename" FontWeight="SemiBold" />
                <TextBox Text="{Binding ProposedFileName, Mode=OneWay}" Style="{StaticResource SelectableDetailText}" Foreground="#245C37" />
                <TextBox Text="{Binding PreviewError, Mode=OneWay}" Style="{StaticResource SelectablePreviewMessageText}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="ImageTileTemplate" DataType="{x:Type vm:ImageItemViewModel}">
            <Border Background="#FAFAF7" CornerRadius="6" Padding="8"
                    MouseRightButtonUp="ImageTile_RightClick"
                    ToolTip="{Binding TagIssueMessage}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="#DDD7CE" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Background" Value="#FAFAF7" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                <Setter Property="Background" Value="#EEF6FF" />
                                <Setter Property="BorderBrush" Value="#1D4ED8" />
                                <Setter Property="BorderThickness" Value="3" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding HasInvalidTags}" Value="True">
                                <Setter Property="BorderBrush" Value="#B42318" />
                                <Setter Property="BorderThickness" Value="3" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="118" />
                        <RowDefinition Height="42" />
                        <RowDefinition Height="18" />
                        <RowDefinition Height="32" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#E8E5DE" CornerRadius="4">
                        <Image Source="{Binding Thumbnail}" Stretch="Uniform" Loaded="ThumbnailImage_Loaded" />
                    </Border>
                    <TextBlock Grid.Row="1" Margin="0,6,0,0" Text="{Binding DisplayName}" TextWrapping="Wrap"
                               FontWeight="SemiBold" MaxHeight="36" LineHeight="16" LineStackingStrategy="BlockLineHeight"
                               ToolTip="{Binding DisplayName}" />
                    <TextBlock Grid.Row="2" Margin="0,2,0,0" Text="{Binding FileName}" TextTrimming="CharacterEllipsis"
                               FontSize="10" LineHeight="13" LineStackingStrategy="BlockLineHeight"
                               Foreground="#666666" ToolTip="{Binding FileName}" />
                    <WrapPanel x:Name="TileStatusBadges" Grid.Row="3" VerticalAlignment="Center" ToolTip="{Binding PreviewError}">
                        <Border Background="#FFF1F0" BorderBrush="#B42318" BorderThickness="1" ToolTip="{Binding TagIssueMessage}">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource TileBadge}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasRenameBlockingIssue}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="!" Foreground="#B42318" FontWeight="Bold" FontSize="11" />
                        </Border>
                        <Border Background="#E9F8EE" BorderBrush="#99D2A7" BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource TileBadge}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCompleteForCurrentBucketPass}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="Ready" Foreground="#245C37" FontWeight="SemiBold" FontSize="11" />
                        </Border>
                        <Border Background="#E9F8EE" BorderBrush="#99D2A7" BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource TileBadge}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAlreadyNormalized}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="Done" Foreground="#245C37" FontWeight="SemiBold" FontSize="11" />
                        </Border>
                        <Border Background="#FFF7E6" BorderBrush="#E4B55C" BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource TileBadge}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasIncompleteTags}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="Needs" Foreground="#8A4D00" FontWeight="SemiBold" FontSize="11" />
                        </Border>
                        <Border Background="#FFF7E6" BorderBrush="#E4B55C" BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource TileBadge}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDirty}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="Changed" Foreground="#8A4D00" FontWeight="SemiBold" FontSize="11" />
                        </Border>
                    </WrapPanel>
                </Grid>
            </Border>
        </DataTemplate>
        <Style x:Key="StatusBorder" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D7D3CA" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsStatusWarning}" Value="True">
                    <Setter Property="Background" Value="#FFF1F0" />
                    <Setter Property="BorderBrush" Value="#B42318" />
                    <Setter Property="BorderThickness" Value="2" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Style="{StaticResource ToolButton}" Margin="0,0,8,0"
                    Content="Choose Folder"
                    Command="{Binding BrowseCommand}" />
            <TextBox Grid.Column="1" MinHeight="32" VerticalContentAlignment="Center"
                     Text="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}" />
            <Grid Grid.Column="2" Margin="8,0,0,0">
                <TextBox MinHeight="32" VerticalContentAlignment="Center"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Search filenames/tags..." Margin="8,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="#777777"
                           IsHitTestVisible="False"
                           Visibility="{Binding FilterText, Converter={StaticResource StringIsNullOrWhiteSpaceToVisibilityConverter}}" />
            </Grid>
            <StackPanel Grid.Column="3" Orientation="Horizontal">
                <Button Style="{StaticResource ScanButton}" Content="Scan Folder" Command="{Binding ScanCommand}" Click="ScanFolder_Click" />
                <Button Style="{StaticResource ToolButton}" Content="Preview Selected" Command="{Binding PreviewRenameCommand}" />
                <Button Style="{StaticResource ToolButton}" Content="Preview Changed" Command="{Binding PreviewChangedCommand}" />
                <Button Style="{StaticResource ToolButton}" Content="Cancel Preview" Command="{Binding CancelPreviewCommand}" />
                <Button Style="{StaticResource ToolButton}" Content="Rename Selected" Command="{Binding RenameSelectedCommand}"
                        ToolTip="{Binding RenameBlockReason}" />
                <Button Style="{StaticResource ToolButton}" Content="Open Logs" Click="OpenLogsFolder_Click" />
            </StackPanel>
        </Grid>

        <TabControl x:Name="MainTabs" Grid.Row="1" SelectionChanged="MainTabs_SelectionChanged">
            <TabItem Header="Library">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="330" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#D7D3CA" BorderThickness="1" CornerRadius="6" Padding="12">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Text="Tags" Style="{StaticResource PanelTitle}" />
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding TagButtonGroups}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:TagButtonGroupViewModel}">
                                            <GroupBox Header="{Binding Name}" Margin="0,0,0,10" Padding="8">
                                                <ItemsControl ItemsSource="{Binding Buttons}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate DataType="{x:Type vm:TagButtonViewModel}">
                                                            <Button Style="{StaticResource TagButton}"
                                                                    Command="{Binding DataContext.ApplyTagCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"
                                                                    ToolTip="{Binding Group}">
                                                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" TextAlignment="Center" HorizontalAlignment="Center" />
                                                            </Button>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel HorizontalAlignment="Center" />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </GroupBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>

                    <Border x:Name="LibraryImageSurface" Grid.Column="1" Margin="12,0" Background="#FFFFFF" BorderBrush="#D7D3CA" BorderThickness="1" CornerRadius="6" Padding="10"
                            PreviewMouseLeftButtonDown="ImageSurface_PreviewMouseLeftButtonDown"
                            PreviewMouseLeftButtonUp="ImageSurface_PreviewMouseLeftButtonUp"
                            MouseMove="ImageSurface_MouseMove"
                            MouseLeave="ImageSurface_MouseLeave"
                            LostMouseCapture="ImageSurface_LostMouseCapture">
                        <Grid Background="Transparent" ClipToBounds="False">
                            <ListBox x:Name="LibraryList"
                                     Margin="0,28,0,0"
                                     ItemsSource="{Binding ItemsView}"
                                     SelectionMode="Extended"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     SelectionChanged="LibraryList_SelectionChanged"
                                     PreviewMouseLeftButtonDown="ImageList_PreviewMouseLeftButtonDown"
                                     PreviewMouseLeftButtonUp="ImageList_PreviewMouseLeftButtonUp"
                                     MouseMove="ImageList_MouseMove"
                                     MouseLeave="ImageList_MouseLeave"
                                     LostMouseCapture="ImageList_LostMouseCapture"
                                     KeyDown="LibraryList_KeyDown"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemContainerStyle>
                                    <StaticResource ResourceKey="TileItem" />
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <StaticResource ResourceKey="ImageTileTemplate" />
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <TextBlock Text="Images"
                                       Style="{StaticResource PanelTitle}"
                                       IsHitTestVisible="False"
                                       Panel.ZIndex="1"
                                       VerticalAlignment="Top"
                                       HorizontalAlignment="Left" />
                        </Grid>
                    </Border>

                    <Border Grid.Column="2" Background="#FFFFFF" BorderBrush="#D7D3CA" BorderThickness="1" CornerRadius="6" Padding="12">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Text="Selection" Style="{StaticResource PanelTitle}" />
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <TextBlock Text="{Binding SelectionCountText}" FontWeight="SemiBold" Margin="0,0,0,10" />
                                    <TextBox Text="{Binding SelectionAggregateText, Mode=OneWay}" Style="{StaticResource SelectableDetailText}" />
                                    <ContentControl Content="{Binding SingleSelectionDetailItem}" ContentTemplate="{StaticResource SelectedImageDetailsTemplate}" />
                                    <Separator Margin="0,12" />
                                    <WrapPanel Margin="0,0,0,12">
                                        <Button Style="{StaticResource ToolButton}" Margin="0,0,6,6" Content="Clear Selected Temporary Tags" Command="{Binding ClearSelectedTemporaryTagsCommand}" />
                                        <Button Style="{StaticResource ToolButton}" Margin="0,0,6,6" Content="Clear All Temporary Tags" Command="{Binding ClearAllTemporaryTagsCommand}" />
                                    </WrapPanel>
                                    <Separator Margin="0,0,0,12" />
                                    <TextBlock Text="{Binding RenameBlockReason}" TextWrapping="Wrap" Foreground="#555555" />
                                </StackPanel>
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Bucket Tagging">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#D7D3CA" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                        <WrapPanel VerticalAlignment="Center">
                            <TextBlock Text="Pass" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <ComboBox Width="170" VerticalContentAlignment="Center" ItemsSource="{Binding BucketPasses}" SelectedItem="{Binding SelectedBucketPass}" />
                            <TextBlock Text="Filter" VerticalAlignment="Center" FontWeight="SemiBold" Margin="16,0,6,0" />
                            <ComboBox Width="130" VerticalContentAlignment="Center" ItemsSource="{Binding BucketFilterModes}" SelectedItem="{Binding SelectedBucketFilter}" />
                            <TextBlock Text="Page size" VerticalAlignment="Center" FontWeight="SemiBold" Margin="16,0,6,0" />
                            <ComboBox Width="80" VerticalContentAlignment="Center" ItemsSource="{Binding BucketPageSizes}" SelectedItem="{Binding BucketPageSize}" />
                            <Button Style="{StaticResource ToolButton}" Content="Previous" Command="{Binding PreviousBucketPageCommand}" />
                            <TextBlock Text="{Binding BucketPageText}" VerticalAlignment="Center" Margin="10,0" />
                            <Button Style="{StaticResource ToolButton}" Content="Next" Command="{Binding NextBucketPageCommand}" />
                            <Button Style="{StaticResource ToolButton}" Content="Refresh Working Set" Command="{Binding RefreshBucketWorkingSetCommand}" />
                            <TextBlock Text="Default" VerticalAlignment="Center" FontWeight="SemiBold" Margin="16,0,6,0" />
                            <ComboBox Width="130" VerticalContentAlignment="Center" ItemsSource="{Binding BucketBuckets}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding SelectedDefaultBucketValue}" />
                            <Button Style="{StaticResource ToolButton}" Content="Apply default to current page" Command="{Binding ApplyDefaultToPageCommand}" />
                            <Button Style="{StaticResource ToolButton}" Content="Invert Selection" Command="{Binding InvertBucketSelectionCommand}" />
                            <Button Style="{StaticResource ToolButton}" Content="Select Untagged Page" Command="{Binding SelectUntaggedBucketCommand}" />
                        </WrapPanel>
                    </Border>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="330" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#D7D3CA" BorderThickness="1" CornerRadius="6" Padding="12">
                            <DockPanel>
                                <TextBlock DockPanel.Dock="Top" Text="Buckets" Style="{StaticResource PanelTitle}" />
                                <TextBlock DockPanel.Dock="Top" Text="{Binding BucketShortcutHintText}" TextWrapping="Wrap" Foreground="#555555" Margin="0,0,0,8" />
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding BucketBuckets}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:BucketDefinitionViewModel}">
                                                <Button Style="{StaticResource BucketButton}"
                                                        Command="{Binding DataContext.ApplyBucketCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        CommandParameter="{Binding}">
                                                    <TextBlock Text="{Binding ButtonLabel}" FontWeight="SemiBold" TextAlignment="Center" HorizontalAlignment="Center" />
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel HorizontalAlignment="Center" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </ScrollViewer>
                            </DockPanel>
                        </Border>

                        <Border x:Name="BucketImageSurface" Grid.Column="1" Margin="12,0" Background="#FFFFFF" BorderBrush="#D7D3CA" BorderThickness="1" CornerRadius="6" Padding="10"
                                PreviewMouseLeftButtonDown="ImageSurface_PreviewMouseLeftButtonDown"
                                PreviewMouseLeftButtonUp="ImageSurface_PreviewMouseLeftButtonUp"
                                MouseMove="ImageSurface_MouseMove"
                                MouseLeave="ImageSurface_MouseLeave"
                                LostMouseCapture="ImageSurface_LostMouseCapture">
                            <Grid Background="Transparent" ClipToBounds="False">
                                <ListBox x:Name="BucketList"
                                         Margin="0,28,0,0"
                                         ItemsSource="{Binding BucketPageItems}"
                                         SelectionMode="Extended"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         SelectionChanged="BucketList_SelectionChanged"
                                         PreviewMouseLeftButtonDown="ImageList_PreviewMouseLeftButtonDown"
                                         PreviewMouseLeftButtonUp="ImageList_PreviewMouseLeftButtonUp"
                                         MouseMove="ImageList_MouseMove"
                                         MouseLeave="ImageList_MouseLeave"
                                         LostMouseCapture="ImageList_LostMouseCapture"
                                         KeyDown="BucketList_KeyDown"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                    <ListBox.ItemContainerStyle>
                                        <StaticResource ResourceKey="TileItem" />
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <StaticResource ResourceKey="ImageTileTemplate" />
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <TextBlock Text="Current Page"
                                           Style="{StaticResource PanelTitle}"
                                           IsHitTestVisible="False"
                                           Panel.ZIndex="1"
                                           VerticalAlignment="Top"
                                           HorizontalAlignment="Left" />
                            </Grid>
                        </Border>

                        <Border Grid.Column="2" Background="#FFFFFF" BorderBrush="#D7D3CA" BorderThickness="1" CornerRadius="6" Padding="12">
                            <DockPanel>
                                <TextBlock DockPanel.Dock="Top" Text="Selection" Style="{StaticResource PanelTitle}" />
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel>
                                        <TextBlock Text="{Binding BucketSelectionCountText}" FontWeight="SemiBold" Margin="0,0,0,10" />
                                        <TextBox Text="{Binding BucketSelectionAggregateText, Mode=OneWay}" Style="{StaticResource SelectableDetailText}" />
                                        <ContentControl Content="{Binding BucketSingleSelectionDetailItem}" ContentTemplate="{StaticResource SelectedImageDetailsTemplate}" />
                                        <Separator Margin="0,12" />
                                        <WrapPanel Margin="0,0,0,12">
                                            <Button Style="{StaticResource ToolButton}" Margin="0,0,6,6" Content="Clear Selected Temporary Tags" Command="{Binding ClearBucketSelectedTemporaryTagsCommand}" />
                                            <Button Style="{StaticResource ToolButton}" Margin="0,0,6,6" Content="Clear Current Bucket Pass Tags" Command="{Binding ClearCurrentBucketPassTagsCommand}" />
                                            <Button Style="{StaticResource ToolButton}" Margin="0,0,6,6" Content="Clear All Temporary Tags" Command="{Binding ClearAllTemporaryTagsCommand}" />
                                        </WrapPanel>
                                        <Separator Margin="0,0,0,12" />
                                        <TextBox Text="{Binding BucketWorkingSetSummary, Mode=OneWay}" Style="{StaticResource SelectableDetailText}" />
                                        <TextBlock Text="{Binding SelectedBucketPass.DisplayName, StringFormat=Current pass: {0}}" FontWeight="SemiBold" Margin="0,0,0,8" />
                                        <TextBlock Text="Ctrl+click, Shift+click, arrow keys, Ctrl+A, and Escape work in the current page." TextWrapping="Wrap" Foreground="#555555" />
                                        <TextBlock Text="Partial bucket tags are saved as work-in-progress app data outside the image folder and outside the repository." TextWrapping="Wrap" Foreground="#555555" Margin="0,8,0,0" />
                                    </StackPanel>
                                </ScrollViewer>
                            </DockPanel>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" Margin="0,12,0,0" Style="{StaticResource StatusBorder}" CornerRadius="6" Padding="10">
            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>
